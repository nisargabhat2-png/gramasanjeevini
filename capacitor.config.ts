import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gramasanjeevini.app',
  appName: 'Grama Sanjeevini',
  webDir: 'dist'
};

export default config;
